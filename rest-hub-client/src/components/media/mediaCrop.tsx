import Image from 'next/image';

import { MEDIA_TYPES } from '@/constants';
import styles from '@/styles/media/mediaPreview.module.css';

interface MediaCropProps {
  preview: string;
  mediaType: string;
}

export default function MediaCrop({ preview, mediaType }: MediaCropProps) {
  return (
  );
}
